name: test

on:
  workflow_dispatch:
  release:
    types:
      - published

jobs:
  build_sdist:
    name: Build SDist
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: true
        
    - uses: lukka/get-cmake@latest
